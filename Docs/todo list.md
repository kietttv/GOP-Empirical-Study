* Slide trình bày ngắn gọn các nội dung chính đã thực hiện, kết quả đạt được và contribution chính của thesis.
* Re-run/reproduce kết quả: sử dụng đúng source code, checkpoint và configuration đã dùng trong báo cáo.
* Quay video quá trình reproduce các kết quả chính, trong đó đặc biệt phải reproduce được E16 (PCC = 0.671). Chuẩn bị bảng đối chiếu Reported result vs. Reproduced result và giải thích nếu có sai khác.
* Chuẩn bị environment, source code, checkpoints và commands/scripts để có thể chạy trực tiếp một vài experiment chính trong buổi gặp.
* Đảm bảo có thể giải thích rõ methodology, các kết quả chính và contribution khi được hỏi.